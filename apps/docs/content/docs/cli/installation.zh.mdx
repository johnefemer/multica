---
title: CLI Installation
description: Install the Agenthost CLI and start the agent daemon.
---

## Installation

### Homebrew (macOS/Linux)

```bash
brew install multica-ai/tap/multica
```

### Build from Source

```bash
git clone https://github.com/multica-ai/multica.git
cd multica
make build
cp server/bin/agenthost /usr/local/bin/agenthost
```

### Download from GitHub Releases

If Homebrew is not available, download the binary directly:

```bash
OS=$(uname -s | tr '[:upper:]' '[:lower:]')   # "darwin" or "linux"
ARCH=$(uname -m)                                # "x86_64" or "arm64"

# Normalize architecture name
if [ "$ARCH" = "x86_64" ]; then
  ARCH="amd64"
fi

# Get the latest release tag from GitHub
LATEST=$(curl -sI https://github.com/multica-ai/multica/releases/latest \
  | grep -i '^location:' | sed 's/.*tag\///' | tr -d '\r\n')

# Download and extract
curl -sL "https://github.com/multica-ai/multica/releases/download/${LATEST}/multica_${OS}_${ARCH}.tar.gz" \
  -o /tmp/agenthost.tar.gz
tar -xzf /tmp/agenthost.tar.gz -C /tmp agenthost
sudo mv /tmp/agenthost /usr/local/bin/agenthost
rm /tmp/agenthost.tar.gz
```

### Update

```bash
brew upgrade multica-ai/tap/multica
```

For install script or manual installs, use:

```bash
agenthost update
```

`agenthost update` auto-detects your installation method and upgrades accordingly.

## Quick Start

```bash
# One command: configure, authenticate, and start the daemon
agenthost setup
```

This configures the CLI for Agenthost Cloud, opens your browser for login, discovers your workspaces, and starts the agent daemon.

For self-hosted servers, use `agenthost setup self-host` instead. See [Self-Hosting](/getting-started/self-hosting) for details.

## Verify

```bash
agenthost daemon status
```

Confirm:
1. Status is `running`
2. At least one agent is listed (e.g. `claude`, `codex`, `gemini`, `opencode`, `openclaw`, `hermes`, or `pi`)
3. At least one workspace is being watched

If the agents list is empty, install at least one supported AI agent CLI:
- [Claude Code](https://docs.anthropic.com/en/docs/claude-code) (`claude`)
- [Codex](https://github.com/openai/codex) (`codex`)
- [Gemini CLI](https://github.com/google-gemini/gemini-cli) (`gemini`)
- OpenCode (`opencode`)
- OpenClaw (`openclaw`)
- Hermes (`hermes`)

Then restart the daemon:

```bash
agenthost daemon stop && agenthost daemon start
```
